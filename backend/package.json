{
  "name": "portfolio-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless CMS backend (PayloadCMS 3) for the portfolio — REST API for the static frontend.",
  "scripts": {
    "dev": "next dev --webpack",
    "build": "next build",
    "start": "next start",
    "generate:types": "payload generate:types",
    "generate:importmap": "payload generate:importmap",
    "seed": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed.ts",
    "seed:articles": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed/articles.ts",
    "seed:projects": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed/projects.ts",
    "seed:documents": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed/documents.ts",
    "seed:social": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed/social.ts",
    "seed:all": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/seed.ts",
    "seed:dry": "npx tsx src/seed/dry.ts",
    "export": "cp payload.db payload.db.$(date +%Y%m%d-%H%M%S).bak 2>/dev/null; npx tsx src/data-sync/cli/export.ts",
    "import": "npx tsx src/data-sync/cli/import.ts",
    "snapshot": "npx tsx src/data-sync/cli/snapshot.ts",
    "snapshot:restore": "npx tsx src/data-sync/cli/snapshot-restore.ts",
    "test": "node --import tsx --test \"src/data-sync/**/*.test.ts\""
  },
  "dependencies": {
    "@payloadcms/db-sqlite": "latest",
    "@payloadcms/next": "latest",
    "@payloadcms/richtext-lexical": "latest",
    "@payloadcms/ui": "latest",
    "adm-zip": "^0.6.0",
    "archiver": "^8.0.0",
    "graphql": "^16.8.2",
    "next": "^16.2.6",
    "payload": "latest",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "@types/archiver": "^8.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.5.0"
  }
}
