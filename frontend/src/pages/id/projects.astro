---
// Indonesian reading zone (sprint-25): translated projects only. UI chrome stays English.
import ProjectsPage from '../../components/pages/ProjectsPage.astro';

export const prerender = false;
---

<ProjectsPage locale="id" />
